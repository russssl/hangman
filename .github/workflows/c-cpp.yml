name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Compile c++
      uses: Eason-S-Lu/CPP-Test-Compile@v1.0
    - name: Lint
      uses: Dal-Papa/actions-uncrustify@v1.2
